spring:
  application:
    name: User
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://${DB_HOST}:3306/microBD
    username: martinTingeso
    password: martinBD
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8091